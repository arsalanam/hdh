"""ML risk stratification.

Predicts each patient's probability of near-term clinical deterioration —
defined as an urgent visit or a critical lab result within the prediction
horizon — from the prior year of visit history. Requires the ``risk`` extra
(``pip install hdh[risk]``).
"""
